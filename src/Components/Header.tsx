import "../Styles/Header.css";

const Header = () => {
  return (
    <header className="header">
      <button>Search</button>
      <h1>TS Cookbook</h1>
      <button>Menu</button>
    </header>
  );
};

export default Header;
