export interface Recipe {
  _id: string;
  title: string;
  description: string;
  createdAt: string;
  updatedAt: string;
  // category?: string;
  // ingredients?: { amount: number; measurement?: string; name: string }[];
  // directions?: string[];
}
